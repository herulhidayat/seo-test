import styled from 'styled-components';
import { font } from './function/_font.styled';

const DivPopins = styled.div`
  ${font({})}
  font-style: normal;
`;
const InputPopins = styled.input`
  ${font({})}
  font-style: normal;
`;

export { DivPopins, InputPopins };
